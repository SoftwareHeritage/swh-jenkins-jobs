stage('Rust tests') {
  {% filter indent(width=2) %}
    {%- include 'includes/agent-docker.groovy.j2' -%}
  {% endfilter %}

  steps {
    sh '''\
      prepare-cargo-cache
      cargo test --all-features
    '''
  }
}
